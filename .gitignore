src/bin/
src/obj/
*.user
*.bat

# Legacy PowerShell prototype - superseded by src/, not tracked
BOMGAR MULTI SCREEN GUI.ps1

# Remote Desktop client installers - not redistributed with this project
*.msi

# Local portable deployment: RdpToolbox.exe plus a copy of the Remote Desktop
# client in msrdc\. Kept out of the repo - no Microsoft binaries are
# redistributed here - and out of src\bin so clean rebuilds do not wipe it.
dist/

# Optional embedded copy of the Remote Desktop client, used for single-file
# builds. Supplied locally, never committed.
src/msrdc.zip
